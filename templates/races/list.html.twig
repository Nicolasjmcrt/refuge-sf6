{% extends 'base.html.twig' %}

{% block title %}Liste des {{ races.name }}{% endblock %}

{% block body %}
    <main class="container">
        <section class="row">
            <div class="col-12">
                <h1>Liste des {{ races.name }}</h1>
            </div>
            {% for animal in animals %}
                <div class="col-3 mb-3">
                    <article class="card h-100">
                        <div class="card-body">
                            <h2 class="card-title text-center">{{ animal.name }}</h2>
                            <p class="card-text text-center">{{ animal.description }}</p>
                           <a href="#" class="btn btn-primary">voir les détails</a> 
                        </div>
                    </article>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock %}
